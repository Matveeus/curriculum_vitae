import React from 'react';
import { Typography } from '@mui/material';

const Home = () => {
  return (
    <Typography variant="h4" sx={{ textAlign: 'center' }}>
      Curriculum Vitae !
    </Typography>
  );
};

export default Home;
