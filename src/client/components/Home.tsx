import React from 'react';
import { Typography } from '@mui/material';

const Home = () => {
  return (
    <div>
      <Typography variant="h4" sx={{ textAlign: 'center' }}>
        Curriculum Vitae !
      </Typography>
    </div>
  );
};

export default Home;
